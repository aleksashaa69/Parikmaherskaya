<Window x:Class="Parikmaherskaya.ZapisiWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Parikmaherskaya"
        mc:Ignorable="d"
        Title="Записи клиентов" Height="750" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="White">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Записи клиентов" 
                       FontSize="36" FontWeight="Bold"
                       Foreground="#212529"
                       VerticalAlignment="Center"/>

            <Button Content="+ Добавить запись"
                    Width="200" Height="50"
                    FontSize="18" FontWeight="SemiBold"
                    Click="AddZapis_Click"
                    Grid.Column="1"
                    HorizontalAlignment="Right"/>
        </Grid>

        <Grid Grid.Row="1" Margin="0,20,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Поиск:" FontSize="16" FontWeight="Medium"
                       VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="SearchBox" Grid.Column="1"
                     FontSize="16" Padding="12,10"
                     TextChanged="Filter_TextChanged"/>

            <TextBlock Text="Статус:" FontSize="16" FontWeight="Medium"
                       VerticalAlignment="Center" Margin="40,0,10,0" Grid.Column="2"/>
            <ComboBox x:Name="StatusFilter" Grid.Column="3"
                      FontSize="16" Padding="12,10"
                      SelectionChanged="Filter_TextChanged">
                <ComboBoxItem Content="Все записи" IsSelected="True"/>
                <ComboBoxItem Content="В ожидании"/>
                <ComboBoxItem Content="Подтверждена"/>
                <ComboBoxItem Content="Завершена"/>
                <ComboBoxItem Content="Неявка"/>
                <ComboBoxItem Content="Перенесена"/>
            </ComboBox>
        </Grid>

        <DataGrid Grid.Row="3"
                  x:Name="ZapisiDataGrid"
                  ItemsSource="{Binding Zapisi}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  RowHeight="60"
                  FontSize="15">

            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Binding="{Binding IdZapisi}" Width="60"/>
                <DataGridTextColumn Header="Дата и время" Binding="{Binding DateTimeFormat}" Width="160"/>
                <DataGridTextColumn Header="Клиент" Binding="{Binding KlientFIO}" Width="240"/>
                <DataGridTextColumn Header="Мастер" Binding="{Binding MasterFIO}" Width="220"/>
                <DataGridTextColumn Header="Услуга" Binding="{Binding Usluga}" Width="*"/>
                <DataGridTextColumn Header="Стоимость" Width="130" SortMemberPath="Stoimost">
                    <DataGridTextColumn.Binding>
                        <Binding Path="StoimostFormat"/>
                    </DataGridTextColumn.Binding>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="Margin" Value="0,0,10,0"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="130"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
